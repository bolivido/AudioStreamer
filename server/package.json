{
  "name": "audio-streamer-server",
  "version": "1.0.0",
  "description": "Server for Audio Streamer - handles file uploads and live streaming",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd .. && npm run build",
    "dev": "nodemon server.js",
    "cleanup": "node cleanup.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "fs-extra": "^11.1.1",
    "path": "^0.12.7",
    "mime-types": "^2.1.35",
    "express-rate-limit": "^6.7.0",
    "helmet": "^6.1.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
